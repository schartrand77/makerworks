#!/usr/bin/env sh
set -eu

# Bump patch version in VERSION and stage it, every commit.
# Requires Python 3. If you insist on suffering, rewrite in bash.

python3 .githooks/bump_version.py
git add VERSION