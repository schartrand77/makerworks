# ── Keep the repo visible; only ignore junk. Do NOT nuke the whole context. ──

# Secrets & envs (allow dev template)
.env
.env.*
!.env.dev

# Keys
keys/
*.pem
*.key

# Python cache and bytecode
**/__pycache__/
*.py[cod]

# Virtualenvs and local builds
.venv/
dist/
build/
.cache/

# Logs & runtime data (mount these as volumes in compose/k8s)
logs/**
uploads/**
*.log

# DB files (local only)
*.sqlite3
*.db

# Alembic version files are optional in the image; Dockerfile ensures dir exists
alembic/versions

# Tests and coverage
.pytest_cache/
.coverage
*.test.*

# Editor/OS noise
.DS_Store
.vscode/
.idea/
*.swp

# Node stuff (if a frontend exists, don’t copy deps)
node_modules/
